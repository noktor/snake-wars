# Netlify deploys the frontend only. Backend is on Railway.
# Set env var SNAKE_WARS_BACKEND_URL in Netlify (Site settings → Environment variables).
[build]
  base    = "frontend"
  publish = "."
  command = "npm install && npm run build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS    = "--legacy-peer-deps"
